import React from "react";

const About = () => (
  <div>
    <p>
      This is a test project using the MERN Stack. // MongoDB, Express, React
      and Node.
    </p>
    <p>
      The main purpose with this project is to showcase an login and to build
      and authentication method that actually works and can store information in
      the DB. <br></br> 
      User is kept logged in via Localstorage until the user logs out and token is done as a JWT.
    </p>
  </div>
);

export default About;
